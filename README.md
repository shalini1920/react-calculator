# React Calculator

A simple calculator built using **React.js** and **plain CSS**.  
This project is designed for **learning React fundamentals** and practicing component-based development.

---

## Features

- Basic arithmetic operations: `+`, `-`, `*`, `/`, `%`
- Clear input (`C`) and delete last character (`⌫`)
- Decimal support (`.`)
- Clean and responsive design

---

## Project Structure

